version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    depends_on:
      - backend

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./src:/quoptuna/src
      - ./uploads:/app/uploads
    environment:
      - DATABASE_URL=sqlite:///./quoptuna.db
      - CORS_ORIGINS=http://localhost:3000,http://localhost:5173
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  # Redis for Celery (future use)
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"

  # PostgreSQL (optional, for production)
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: quoptuna
  #     POSTGRES_PASSWORD: quoptuna
  #     POSTGRES_DB: quoptuna
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:
